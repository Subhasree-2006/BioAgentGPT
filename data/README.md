# Data

This folder contains sample data used by BioAgentGPT.

## Files

- `sample_pubmed_results.csv` – Sample PubMed search results.
- `sample_query.txt` – Example research questions.

**Note:** Large generated files such as FAISS indexes (`.faiss`) and serialized objects (`.pkl`) are not included because they can be regenerated by running the notebook.
